## 動機

<!-- 指向 PLAN.md 的條目或里程碑 -->

## 變更

- 

## 驗收證據

<!-- 指令與輸出；碰到輸出契約要貼 exit code -->

## 檢查清單

- [ ] `PLAN.md` 已同步（行為變更時）
- [ ] `go vet` / `go test ./...` 綠
- [ ] 對活 gateway 跑過 `docs/quality.md` 的 Eval 清單（碰 gw/cli 層時）
- [ ] `skills/` 與 `AGENTS.md` 已同步（行為變更時）
- [ ] commit 無 co-author、無 AI 署名
